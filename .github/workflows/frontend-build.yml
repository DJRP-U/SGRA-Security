name: Build Frontend

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: SGRA-WEB/package-lock.json

      - name: Install dependencies (SGRA-WEB)
        working-directory: SGRA-WEB
        run: npm ci

      - name: Build (SGRA-WEB)
        working-directory: SGRA-WEB
        run: npm run build
